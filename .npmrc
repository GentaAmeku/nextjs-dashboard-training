# VitePress + vitepress-plugin-mermaid 対応:
# mermaid の依存(dayjs / cytoscape / @braintree/sanitize-url など)を
# node_modules のトップへ hoist する。これがないと Vite(dev) が依存を解決できず
# 「dayjs does not provide an export named 'default'」で docs:dev が真っ白になる。
shamefully-hoist=true
